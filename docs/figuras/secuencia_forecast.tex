% Diagrama de Secuencia UML — Predicción de Demanda via AMS
\documentclass[tikz, border=14pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows.meta}
\usepackage[T1]{fontenc}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}

\begin{document}
\begin{tikzpicture}[font=\sffamily,
  arr/.style={->, line width=0.9pt, -{Latex[length=2.5mm]}},
  ret/.style={->, line width=0.8pt, dashed, -{Latex[length=2.5mm]}},
  lbl/.style={font=\footnotesize\sffamily, fill=white, inner sep=1pt},
]

% ── X positions ───────────────────────────────────────────────
% usr=0  dash=4.5  api=9.5  ams=14.5  db=19.5

% ─────────────────────────────────────────────────────────────
%  CABECERAS
% ─────────────────────────────────────────────────────────────
\node[draw, line width=0.8pt, fill=gray!15,
      minimum width=3.6cm, minimum height=0.8cm,
      align=center, font=\small\sffamily\bfseries] at (0,0)    {Usuario};
\node[draw, line width=0.8pt, fill=gray!15,
      minimum width=3.6cm, minimum height=0.8cm,
      align=center, font=\small\sffamily\bfseries] at (4.5,0)  {Dashboard\\(React)};
\node[draw, line width=0.8pt, fill=gray!15,
      minimum width=3.6cm, minimum height=0.8cm,
      align=center, font=\small\sffamily\bfseries] at (9.5,0)  {FastAPI\\(API REST)};
\node[draw, line width=0.8pt, fill=gray!15,
      minimum width=3.6cm, minimum height=0.8cm,
      align=center, font=\small\sffamily\bfseries] at (14.5,0) {Módulo AMS};
\node[draw, line width=0.8pt, fill=gray!15,
      minimum width=3.6cm, minimum height=0.8cm,
      align=center, font=\small\sffamily\bfseries] at (19.5,0) {PostgreSQL\\(Supabase)};

% ─────────────────────────────────────────────────────────────
%  LÍNEAS DE VIDA
% ─────────────────────────────────────────────────────────────
\foreach \x in {0, 4.5, 9.5, 14.5, 19.5}
  \draw[dashed, line width=0.7pt] (\x,-0.4) -- (\x,-10.4);

% ─────────────────────────────────────────────────────────────
%  ACTIVACIONES
% ─────────────────────────────────────────────────────────────
\draw[draw=black, line width=0.7pt, fill=white] (4.28,-0.7) rectangle (4.72,-9.75);  % Dashboard
\draw[draw=black, line width=0.7pt, fill=white] (9.28,-1.3) rectangle (9.72,-9.15);  % API
\draw[draw=black, line width=0.7pt, fill=white] (14.28,-4.80) rectangle (14.72,-8.30); % AMS
\draw[draw=black, line width=0.7pt, fill=white] (19.28,-2.35) rectangle (19.72,-4.40); % DB

% ─────────────────────────────────────────────────────────────
%  MENSAJES
% ─────────────────────────────────────────────────────────────

% 1. Usuario → Dashboard
\draw[arr] (0.2,-0.90) -- (4.28,-0.90)
  node[midway, above, lbl] {Selecciona SKU};

% 2. Dashboard → API
\draw[arr] (4.72,-1.55) -- (9.28,-1.55)
  node[midway, above, lbl] {POST /forecast/predict \{sku, horizon\}};

% 3. API → DB
\draw[arr] (9.72,-2.35) -- (19.28,-2.35)
  node[midway, above, lbl] {SELECT sales\_history WHERE family=sku};

% 4. DB → API
\draw[ret] (19.28,-3.05) -- (9.72,-3.05)
  node[midway, above, lbl] {Serie temporal (date, sales)};

% 5. API → DB
\draw[arr] (9.72,-3.75) -- (19.28,-3.75)
  node[midway, above, lbl] {SELECT oil\_prices, holidays};

% 6. DB → API
\draw[ret] (19.28,-4.40) -- (9.72,-4.40)
  node[midway, above, lbl] {Features exógenas};

% 7. API → AMS
\draw[arr] (9.72,-5.05) -- (14.28,-5.05)
  node[midway, above, lbl] {AMS.select(series, features, horizon)};

% ── BLOQUE LOOP (rectángulo exterior) ────────────────────────
\draw[draw=black!70, dashed, line width=0.8pt, rounded corners=3pt]
  (13.5,-5.25) rectangle (21.5,-8.30);

% Self-messages  (primer cuadrito empieza a -5.90, por debajo del tab)
\foreach \y/\lbl in {
  -5.90/{fit + MAPE(ARIMA)},
  -6.45/{fit + MAPE(Prophet)},
  -7.00/{fit + MAPE(XGBoost)},
  -7.55/{fit + MAPE(LSTM)}}{
  \draw[draw=black, line width=0.6pt, fill=white]
    (14.72,\y) rectangle (15.05,\y+0.40);
  \node[font=\footnotesize\sffamily, anchor=west] at (15.10,\y+0.20) {\lbl};
}

% 8. AMS → API
\draw[ret] (14.28,-8.50) -- (9.72,-8.50)
  node[midway, above, lbl] {ForecastResponse \{modelo, MAPE, puntos\}};

% 9. API → Dashboard
\draw[ret] (9.28,-9.15) -- (4.72,-9.15)
  node[midway, above, lbl] {HTTP 200 — JSON ForecastResponse};

% 10. Dashboard → Usuario
\draw[arr] (4.28,-9.75) -- (0.2,-9.75)
  node[midway, above, lbl] {Muestra gráfico de predicción};

% ─────────────────────────────────────────────────────────────
%  NÚMEROS DE PASO
% ─────────────────────────────────────────────────────────────
\foreach \y/\n in {
  -0.90/1, -1.55/2, -2.35/3, -3.05/4, -3.75/5,
  -4.40/6, -5.05/7, -8.50/8, -9.15/9, -9.75/10}
  \node[font=\footnotesize\sffamily, text=gray!70] at (-1.0,\y) {\n};

% ── Tab del loop (caja UML en esquina superior, dibujada al final) ─
\draw[draw=black!70, line width=0.7pt, fill=white]
  (13.5,-5.25) rectangle (17.0,-5.60);
\node[font=\footnotesize\sffamily\itshape]
  at (15.25,-5.425) {loop [cada modelo]};

\end{tikzpicture}
\end{document}
